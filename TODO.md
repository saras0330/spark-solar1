# TODO
- [x] Fix theme conflicts (ThemeProvider + ThemeToggle) so background and card text update correctly

- [ ] Ensure dark-mode styling is consistent for sections that mix CSS variables and Tailwind `dark:` utilities
- [ ] Wire up Solar Calculator button to compute & display results (without changing UI/copy)
- [ ] Add anchor scroll offset handling if navbar hides headings
- [ ] Run build/dev and verify: navbar anchors, theme toggle persistence, FAQ expand/collapse, calculator button, testimonials images

